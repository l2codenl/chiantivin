= render :partial => "menu/category"
#page.grid_9
  %h1=@wine.title
  = image_tag @wine.image.url(:image)
  =raw(@wine.body)
  .shop
    =form_for(@wine, :url => cart_path(Wine.new)) do |f|
      %p
        =text_field_tag :quantity
