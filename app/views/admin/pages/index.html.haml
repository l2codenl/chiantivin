%h1 Pages

%ul
  %li=link_to t(:new, :scope => :page), new_admin_page_path

%table
  %tr
    %th Name
    %th Options
    %th On Homepage?
  - @pages.each do |page|
    %tr
      %td=page.title
      %td
        =link_to t(:edit), edit_admin_page_path(page)
        =link_to t(:delete), admin_page_path(page), :method => "delete"
      %td
        =form_for [:admin, page] do |f|
          -f.check_box :menu, :class => "addmenu"
